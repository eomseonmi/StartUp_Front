import React from "react";
import ShowGiftcard from "../Presentational/ShowGiftcard";

const ShowGiftcardContainer = () => {
  const loadData = async () => {
    // const response = await homeAPI.get();
    console.log('Startup main container load data rse');
  }

  return <ShowGiftcard />
}

export default ShowGiftcardContainer;
